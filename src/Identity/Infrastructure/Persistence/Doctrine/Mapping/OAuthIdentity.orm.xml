<?xml version="1.0" encoding="UTF-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <entity name="App\Identity\Domain\Model\OAuthIdentity" table="user_oauth_identities">
        <id name="id" type="oauth_identity_id" column="id">
            <generator strategy="NONE"/>
        </id>

        <field name="provider" type="string" column="provider" length="20"
               enum-type="App\Identity\Domain\Model\OAuthProvider"/>
        <field name="externalId" type="string" column="external_id" length="191"/>
        <field name="linkedAt" type="datetime_immutable" column="linked_at"/>

        <many-to-one field="user"
                     target-entity="App\Identity\Domain\Model\User"
                     inversed-by="oauthIdentities">
            <join-column name="user_id" referenced-column-name="id" nullable="false" on-delete="CASCADE"/>
        </many-to-one>

        <unique-constraints>
            <!-- Un compte Google donné ne peut ouvrir qu'un seul compte Focusyn. -->
            <unique-constraint name="uniq_oauth_provider_external" columns="provider,external_id"/>
            <!-- Et un compte Focusyn n'a qu'un rattachement par fournisseur. -->
            <unique-constraint name="uniq_oauth_user_provider" columns="user_id,provider"/>
        </unique-constraints>
    </entity>
</doctrine-mapping>
